# Warnings

Warnings are emitted with
`coo_cmd_runtime_emit(command_runtime_get(), &args)` using
`COO_CMD_RUNTIME_EMIT_WARNING`. Reusable warning JSON construction, delivery
selection, and queue emission are documented under the common command-dispatch
helpers.
