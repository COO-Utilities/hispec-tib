# Photodiode Monitoring

The sampler supplies fresh 20 Hz readings; throughput uses those directly.
Diagnostic rolling windows remain available independently. See
[the sampling, synchronization, and uncertainty audit](../photodiode_notes.md).

```{eval-rst}
.. doxygenfile:: app/src/photodiode.h
   :project: hispec_tib
```
