# Enable MCUboot as the bootloader image for sysbuild OTA builds.
SB_CONFIG_BOOTLOADER_MCUBOOT=y
# Observatory OTA deliberately uses MCUboot hash-only images. This avoids
# field key-management failure modes; it does not provide authenticity.
SB_CONFIG_BOOT_SIGNATURE_TYPE_NONE=y
