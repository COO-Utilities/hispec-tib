# Documentation Moved

The hardware source of truth moved to `../../doc/hardware.md`.

Keep firmware/devicetree hardware reconciliation notes in
`../../doc/human_review_required.md`; do not add a second live hardware copy
under `app/doc`.
