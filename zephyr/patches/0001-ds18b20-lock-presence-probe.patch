diff --git a/drivers/sensor/maxim/ds18b20/ds18b20.c b/drivers/sensor/maxim/ds18b20/ds18b20.c
index 8596f6ea47f..1b956dbb540 100644
--- a/drivers/sensor/maxim/ds18b20/ds18b20.c
+++ b/drivers/sensor/maxim/ds18b20/ds18b20.c
@@ -261,7 +261,11 @@ static int ds18b20_configure(const struct device *dev)
 	struct ds18b20_data *data = dev->data;
 	int ret;
 
-	if (w1_reset_bus(cfg->bus) <= 0) {
+	/* The raw presence probe does not acquire the master's bus lock. */
+	w1_lock_bus(cfg->bus);
+	ret = w1_reset_bus(cfg->bus);
+	w1_unlock_bus(cfg->bus);
+	if (ret <= 0) {
 		LOG_ERR("No 1-Wire slaves connected");
 		return -ENODEV;
 	}
